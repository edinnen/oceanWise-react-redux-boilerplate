// import { fromJS } from 'immutable';
// import { selectCmspageDomain } from '../selectors';

describe('selectCmspageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
